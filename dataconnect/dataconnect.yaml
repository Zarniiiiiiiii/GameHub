specVersion: "v1"
serviceId: "gamehub"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gamehub-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
